(setq mew-name "Tomoya Miwa") ;; (user-full-name)
(setq mew-user "miwa@sassor.com") ;; (user-login-name)
(setq mew-mail-domain "sassor.com")
(setq mew-smtp-server "sassor.com")  ;; if not localhost
;; (setq mew-pop-user "your POP account")  ;; (user-login-name)
(setq mew-pop-server "sassor.com")    ;; if not localhost
(setq mew-use-cached-passwd t)
